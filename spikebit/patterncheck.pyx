"""
Created on Sun Jul  2 19:02:44 2017

@author: bengt
"""
import numpy as np
import gmpy2
from gmpy2 import mpz

def checkone(D,aPattern):
    """ 
    patterncheck(D,aPattern) 
    Checks bitwise if all bits of the required pattern aPattern are set in 
    array D. Returns ratio of bits in checked pattern that are matching.
    """
    # TODO finish this and implement pattern
    np.bitwise_and()
    np.hamming()
    np.equal
    a=mpz(3)

def checkPrevious()    